```bash
cd rust
cargo build --release
```

**Prefix Sum (Programmability)**:
```bash
cargo run --release --bin prefix_sum
```

**Matrix Multiply (Scalability)**:
```bash
cargo run --release --bin matrix_multiply
cargo run --release --bin matrix_multiply 1024 8
```